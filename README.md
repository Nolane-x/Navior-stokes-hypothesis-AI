# Navier–Stokes Hypothesis AI

A falsification-first, machine-auditable research program for the 3D incompressible Navier–Stokes Millennium Prize Problem, orchestrated with Nolane World 0.5.0 at depth W5.

## Status

**NOT SOLVED.** This repository contains verified partial structural theorems, explicit no-go results, independent challengers, counterexamples and reproducible certificates. None is a Clay solution.

Primary target: Clay statement **(B)** — global existence and smoothness on `R^3/Z^3` for every smooth divergence-free periodic initial velocity and zero forcing.

Official problem description: https://www.claymath.org/wp-content/uploads/2022/06/navierstokes.pdf

## Current checkpoint

Latest canonical semantic checkpoint:

> `checkpoints/W5-E42-SEMANTIC-LEDGER.md`

Final E42 aggregate verification is self-reporting. GitHub Actions run

> `32085926354`

checked theorem/checker HEAD

> `15175874a90ea42c765c8bf675cd611ffde32475`

and recorded

```text
verification_scripts=55
PASS verification_scripts=55
scope=PARTIAL_CERTIFICATES_ONLY_NOT_GLOBAL_REGULARITY
```

in `verification/W5_E42_full_suite_result.txt`.

Current Nolane World continuation state:

> `world_ed29477b4f5b4345`

Depth: `W5`.

Its convergence gate **FAILED intentionally**. One mathematical critical unknown remains, together with unresolved high-severity attacks/contradictions and process-depth requirements. World scores are research-governance diagnostics, **not percentages of the Millennium Problem solved**.

## Canonical frame

For zero forcing the spatial mean is conserved. The exact Galilean transform

```text
v(x,t) = u(x + m t,t) - m
```

reduces the periodic regularity problem to the canonical zero-mean frame. Speed-based diagnostics in the proof spine are frozen in this frame; R21/C001 record the theorem and its frame-sensitive falsifier.

## Proof spine in one page

Set

```text
L = omega × u,
G = |u|u,
U = ||u||_(3/2).
```

The project began by isolating the scale-critical `L^3` pressure/Lamb obstruction and then progressively removed false or representation-dependent escape mechanisms.

### R01–R13 — critical endpoint and physical UV reduction

The exact pressure-work identities are

```text
W_3 = <Q L,Q G> = -<P L,P G>.
```

R01–R08 isolate the critical `L^3` obstruction. R18 removes cutoff-generated UV contamination and proves that the surviving mechanism must escape through the **physical untruncated Lamb force**. R20 proves that high Lamb output cannot be generated by two velocity inputs both confined below half the output frequency.

### R14–R20 — helical conflict / spectral geometry

The physical Lamb force is factored through spin conflict and radial bandwidth. A monochromatic single-spin Beltrami field is an exact zero-Lamb endpoint of the factorization. The surviving UV mechanism therefore requires spin conflict and/or spectral dispersion.

### R21–R25 — pressure representation cleanup

After Galilean zero-mean normalization, componentwise flux on regular iso-speed components vanishes. The pressure obstruction is rewritten in bulk form, then as a commutator/transported-speed object:

```text
Q(|u|u) = [Q,|u|]u,
q_amp = div(|u|u)
      = u·grad|u|
      = u^T S u / |u|.
```

R25 proves every fixed finite output range contributes only integrable pressure work. A singular mechanism must run to genuine ultraviolet output.

### R26–R34 — balanced dual-channel and terminal-packet structure

R27 introduces

```text
A_bal = ∫ U min(||P L||_2^2, ||Q L||_2^2) dt.
```

Finite `A_bal` controls the same `L^3` endpoint barrier. Thus even the pointwise weaker Helmholtz Lamb channel must become non-summable under a hypothetical singular endpoint.

R28–R34 show that both exact pressure-work representations must become productively ultraviolet and asymptotically synchronized. R31/R32 extract shrinking terminal packets and control the representation mismatch by the actual terminal enstrophy tail.

### R35–R38 — common mode, helical P/Q geometry and hierarchy synchronization

E37 corrected an important strategic mistake: making the P/Q **mismatch** small does not control their common productive mode.

With `J=Q-P`, R37 identifies

```text
2 C_H = <H L,J H G>.
```

For helical pair `p,q -> k`, the pair-symmetrized Lamb coefficient contains the exact factor

```text
s|p| - t|q|.
```

R37 derives closed P/Q channel norms. Consequences include:

- same-spin equal-shell pairs vanish exactly;
- same-spin near-equal-shell Q output has quadratic radial-gap depletion;
- opposite-spin equal-shell pairs remain gradient-strong but their solenoidal channel is depleted near collinearity.

R38 then extracts one terminal packet on which **every cutoff in a prescribed expanding hierarchy** is simultaneously productive and P/Q-synchronized. Every signed sharp shell in that hierarchy is synchronized too.

Pairwise helical depletion still does not automatically sum through the nonlinear many-body balanced minimum.

## E42: from UV tail to normalized high-dimensional bursts

E42 deliberately attacks the remaining common-mode accumulation rather than refining the old mismatch again.

### R39 — spacetime total-variation synchronization on finite catalogs

For output mode `k`, define

```text
w_grad,k = V Re[(Q_k Lhat(k))·conj(Q_k Ghat(k))],
w_sol,k  = -V Re[(P_k Lhat(k))·conj(P_k Ghat(k))].
```

Then

```text
d_k := w_grad,k-w_sol,k
     = V Re[Lhat(k)·conj(Ghat(k))].
```

The modewise coefficient bound yields, for every finite catalog `F` and terminal interval `I=[a,b]`,

```text
∫_I Σ_(k∈F)|d_k|dt
 <= V^-1 |F| E0^3 sqrt(|I| q(a)),
q(a)=∫_a^{T*}||omega||_2^2dt.
```

Thus the two representations synchronize in genuine `L^1_t ell^1_k` total variation on any prescribed finite catalog, even when the catalog grows arbitrarily from stage to stage.

R39-C upgrades this to product-measure/Jordan synchronization: arbitrary time×mode subsets, positive work, negative work and the resolved common mode all inherit the same total-variation control.

### RD019 — TV synchronization is not tightness

An abstract perfectly synchronized work atom may still escape to one mode `kappa_n -> infinity`. Therefore P/Q agreement by itself does not yield compactness or common-mode smallness.

RD019 is a logical countermodel, **not** an NS blow-up construction.

### R40 — absolute work evacuates every prescribed growing finite catalog

The coefficient estimate applies separately to both exact representations:

```text
|w_grad,k|, |w_sol,k|
 <= V^-1 E0^3 ||omega||_2.
```

Hence the terminal extraction can be chosen so that

```text
∫_(I_n) Σ_(0<|k|<=L_n)
 (|w_grad,k|+|w_sol,k|)dt -> 0,
```

while simultaneously

```text
∫_(I_n) W_grad,>L_n dt >= M_n -> infinity,
∫_(I_n) W_sol,>L_n  dt >= M_n -> infinity.
```

The common productive mode is therefore forced out of every prescribed growing resolved prefix in **absolute work**, not merely in signed mismatch.

### RD020 — energy/enstrophy budgets alone cannot stop the terminal cascade

A parabolic critical burst at frequency `N` has the schematic costs

```text
duration                  ~ N^-2,
integrated enstrophy      ~ N^-1,
critical integrated work  ~ N^0.
```

A lacunary sequence such as `N_j=2^(j^2)` can therefore have finite total energy/enstrophy/time cost but infinitely many `O(1)` critical-work bursts.

RD020 is an abstract scaling-budget route guard, not an NS solution. It proves that a closing theorem must exploit genuine PDE coupling/rigidity rather than first-energy accounting plus UV evacuation.

### R41 — normalized unit common-work bursts

On an R40 parent packet define the high-pass common work

```text
C_L(t) = [W_grad,>L(t)+W_sol,>L(t)]/2.
```

First hitting times of successive integer levels of

```text
F(t)=∫ C_L
```

extract disjoint subintervals `J_j` satisfying exactly

```text
∫_(J_j) C_L dt = 1.
```

No monotonicity or positivity of instantaneous work is assumed; the verifier stresses deep signed backtracking.

R39 gives on every unit burst

```text
∫ W_grad,>L = 1+o(1),
∫ W_sol,>L  = 1+o(1),
```

and R40 makes resolved absolute work negligible.

At least half of the unit bursts in each parent packet simultaneously obey

```text
|J_j| <= 4 |I|/N,
∫_(J_j)||omega||_2^2dt <= 4 q_I/N.
```

Thus a hypothetical singularity produces **actual-trajectory normalized unit critical-work bursts with vanishing duration and vanishing unweighted viscous cost**.

### R42 — spectral multiplicity explosion

Let

```text
c_k(t) = [w_grad,k(t)+w_sol,k(t)]/2,
b_k(J) = ∫_J c_k(t)dt.
```

Each individual output mode satisfies

```text
|b_k(J)|
 <= beta_J
 := V^-1 E0^3 sqrt(|J| q_J).
```

For a unit burst

```text
Σ_(|k|>L) b_k(J)=1.
```

Let `m_theta(J,L)` be the smallest number of high output modes whose **positive** integrated common work carries a fixed fraction `theta` of one unit. Then

```text
m_theta(J,L)
 >= theta / beta_J
 = theta V/[E0^3 sqrt(|J|q_J)].
```

For the R41 good bursts this becomes

```text
m_theta
 >= theta V N/[4E0^3 sqrt(|I|q_I)]
 -> infinity.
```

Therefore the singular mechanism cannot escape as one Fourier mode or any bounded-sparse family. It must be both

1. **frequency escaping**, and
2. **spectrally high-dimensional**.

This is the first quantitative many-output theorem in the post-R37 proof spine.

## Independent spatial challenger

`research/C03_atomic_endpoint_full_tail_rigidity_challenger.md` introduces a deliberately independent route based on Hao Huang's 2026 preprint `arXiv:2608.04138`.

The preprint's stated atomic branch uses the endpoint kinetic-energy measure, local Hodge packets, same-parent Oseen evolution and a backward adjoint to derive full-tail rigidity and a delayed second-order budget obstruction.

The repository does **not** import that preprint as an internal proof step, and R41–R42 do not imply an endpoint atom. C03 is a challenger/route selector:

- atomic endpoint branch → test against spatial/Oseen full-tail rigidity;
- diffuse endpoint branch → seek diffuse critical compactness/spatial rigidity.

## Current E42 obstruction

A hypothetical singular trajectory compatible with every verified partial theorem must now support a terminal sequence with all of the following simultaneously:

1. exactly one normalized unit of common high-pass pressure work per selected burst;
2. asymptotically the same unit work in both exact Helmholtz representations;
3. negligible absolute work below an arbitrarily prescribed growing cutoff;
4. vanishing burst duration;
5. vanishing unweighted enstrophy cost;
6. genuine high-frequency velocity ancestry;
7. R37 spin/radial/angular pair restrictions;
8. an exploding number of productive high output modes;
9. coherent many-triad accumulation across those modes;
10. compatibility with an atomic or diffuse spatial endpoint branch.

The live mathematical object is therefore

> **a normalized, frequency-escaping, spectrally high-dimensional common-work burst whose many helical interactions remain coherent despite vanishing unweighted viscous cost.**

## Current missing theorem

The next load-bearing theorem must not be another P/Q synchronization estimate or generic energy/Hölder bound. It must provide one of:

- an **intrinsic active scale** and a scale-time law for each R41/R42 burst;
- a **many-body geometric depletion theorem** converting R37 helical factors + R42 multiplicity into a summable critical bound;
- a **spatial compactness/rigidity theorem** selecting a center, producing a nontrivial normalized limit and excluding it;
- an **inter-burst PDE rigidity theorem** ruling out the RD020-style infinite sequence of increasingly fine cheap critical bursts.

No such arbitrary-data closing theorem is currently proved.

## Verification

Final E42 aggregate:

```text
workflow run: 32085926354
verification_scripts=55
PASS verification_scripts=55
```

The self-report file is `verification/W5_E42_full_suite_result.txt`.

Representative new verifier volumes:

```text
R39 primary                     126068 checks
R40 primary                     143588 checks
R40 fresh physical/projector     98200 checks
R39/R40 exact rational          223000 checks
R41 primary                     284919 checks
R41 fresh exact/backtracking    313361 checks
R42 primary                     285020 checks
R42 fresh exact rational        115997 checks
```

World gate: `verification/W5_E42_world_gate_result.json` — **FAILED / NONCONVERGED_PARTIALS_ONLY**.

## Research integrity

The repository follows `docs/research_protocol.md`. In particular:

- finite computation never implies the continuum PDE theorem;
- a regularity criterion is not a proof until its hypothesis is obtained a priori;
- energy/enstrophy alone is not promoted into critical control;
- negative results and route guards remain first-class artifacts;
- pairwise helical cancellation is not silently promoted to a many-body inequality;
- P/Q synchronization is not confused with common-mode smallness;
- an external preprint is a challenger unless independently reconstructed;
- internal World metrics are never presented as percentages of the Millennium Problem solved.

## Primary-source interfaces

- Clay/Fefferman official problem description: https://www.claymath.org/wp-content/uploads/2022/06/navierstokes.pdf
- T. Tao, *Finite time blowup for an averaged three-dimensional Navier-Stokes equation*: https://arxiv.org/abs/1402.0290
- A. Vasseur, *Regularity criterion for 3D Navier-Stokes equations in terms of the direction of the velocity*: https://arxiv.org/abs/0705.2446
- J. Lerner, N. Vigneron, *On some properties of the curl operator and their consequences for the Navier-Stokes system*: https://arxiv.org/abs/2203.07950
- Z. Grujic, Z. Bradshaw, *Frequency localized regularity criteria for the 3D Navier-Stokes equations*: https://arxiv.org/abs/1501.01043
- A. Cheskidov, M. Dai, *Regularity criteria for the 3D Navier-Stokes and MHD equations*: https://arxiv.org/abs/1507.06611
- H. Huang, *Full-Tail Dynamical Rigidity Forced by Atomic Navier-Stokes Energy Concentration*: https://arxiv.org/abs/2608.04138

## Repository layout

```text
docs/          protocols, problem certificates, dependency maps
research/      verified partial theorems, challengers and structural reductions
corrections/   explicit scope/frontier corrections
discarded/     falsified routes and exact obstructions
experiments/   preregistered or frozen computational experiments
verification/  exact/symbolic/fresh-context reproduction certificates
checkpoints/   milestone semantic ledgers
```

The project advances only when a result survives its declared verification scope.
